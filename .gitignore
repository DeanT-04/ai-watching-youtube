# Scratch, deliverable, store, and Q&A archive directories — never commit these.
# (anchored to the repo root so internal/store/ is never matched)
work/
output/
/store/
results/

# Build artifacts
/ytreconstruct.exe
/ytreconstruct
/bin/

# Editor / OS cruft
.reasonix/
.DS_Store
Thumbs.db

# Local whisper models are fetched by the user, never committed
*.bin
